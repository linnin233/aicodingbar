<UserControl x:Class="AiCodingBar.Dashboard.SessionGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="700">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Session 状态" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            <CheckBox x:Name="PinCheckBox" Content="固定窗口" Margin="16,0,0,0" VerticalAlignment="Center"/>
            <ComboBox x:Name="FilterCombo" Margin="16,0,0,0" Width="120" SelectedIndex="0">
                <ComboBoxItem>全部</ComboBoxItem>
                <ComboBoxItem>活跃</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <DataGrid x:Name="SessionDataGrid" Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserSortColumns="True"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" Binding="{Binding SortIndex}" Width="50"/>
                <DataGridTextColumn Header="Session ID" Binding="{Binding SessionId}" Width="120"/>
                <DataGridTextColumn Header="名称" Binding="{Binding SessionTitle}" Width="140"/>
                <DataGridTemplateColumn Header="状态" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Status}" FontWeight="Bold"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="持续时间" Width="100">
                    <DataGridTextColumn.Binding>
                        <MultiBinding StringFormat="{}{0}">
                            <Binding Path="LastUpdateAt"/>
                            <Binding Path="StartedAt"/>
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="最近事件" Binding="{Binding LastEvent}" Width="120"/>
                <DataGridTextColumn Header="工具" Binding="{Binding ToolName}" Width="80"/>
                <DataGridTextColumn Header="CWD" Binding="{Binding Cwd}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Margin="0,6,0,0" Foreground="Gray">
            <Run Text="共 "/><Run x:Name="CountText" Text="0"/><Run Text=" 个 session"/>
        </TextBlock>
    </Grid>
</UserControl>
