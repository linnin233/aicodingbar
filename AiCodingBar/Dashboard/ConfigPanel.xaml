<UserControl x:Class="AiCodingBar.Dashboard.ConfigPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="状态映射配置" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="任务栏默认模式:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="ModeCombo" Width="120" SelectedIndex="0">
                    <ComboBoxItem>compact</ComboBoxItem>
                    <ComboBoxItem>aggregate</ComboBoxItem>
                    <ComboBoxItem>highlight</ComboBoxItem>
                </ComboBox>
                <TextBlock Text=" 自动切换阈值:" VerticalAlignment="Center" Margin="16,0,8,0"/>
                <TextBox x:Name="ThresholdBox" Width="60" Text="7"/>
                <CheckBox x:Name="ShowZeroCheckBox" Content="聚合模式显示零值" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </StackPanel>
        </StackPanel>

        <DataGrid x:Name="MappingGrid" Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Hook 事件" Binding="{Binding Key}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="状态名" Binding="{Binding Value.State}" Width="80"/>
                <DataGridTextColumn Header="中文名" Binding="{Binding Value.Name}" Width="80"/>
                <DataGridTextColumn Header="缩写" Binding="{Binding Value.Abbr}" Width="60"/>
                <DataGridTextColumn Header="颜色" Binding="{Binding Value.Color}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal" Margin="0,8,0,4">
                <Button x:Name="SaveBtn" Content="保存配置" Width="100" Margin="0,0,8,0"/>
                <Button x:Name="ResetBtn" Content="恢复默认" Width="100"/>
            </StackPanel>
            <Separator Margin="0,6,0,4"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="HookStatusText" Text="Hook 状态: 检测中..." VerticalAlignment="Center" Margin="0,0,12,0"/>
                <Button x:Name="InstallHooksBtn" Content="安装 Hook" Width="100" Margin="0,0,8,0"/>
                <Button x:Name="UninstallHooksBtn" Content="卸载 Hook" Width="100"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
