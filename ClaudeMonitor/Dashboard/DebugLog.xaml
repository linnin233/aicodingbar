<UserControl x:Class="ClaudeMonitor.Dashboard.DebugLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
            <Button x:Name="ClearBtn" Content="清空" Width="60" Margin="0,0,8,0"/>
            <Button x:Name="TestBtn" Content="模拟事件" Width="80" Margin="0,0,8,0"/>
            <CheckBox x:Name="AutoScrollCheck" Content="自动滚动" IsChecked="True" VerticalAlignment="Center"/>
            <TextBlock x:Name="StatusText" Text="服务器状态: --" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <RichTextBox x:Name="LogBox" Grid.Row="1" IsReadOnly="True"
                     FontFamily="Consolas" FontSize="11"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="#1E1E1E" Foreground="#D4D4D4">
        </RichTextBox>

        <Grid Grid.Row="2" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="QuickInput" Text="http://127.0.0.1:23333/state" FontFamily="Consolas" FontSize="11"/>
            <Button x:Name="SendBtn" Grid.Column="1" Content="发送 GET" Width="70" Margin="4,0,0,0"/>
        </Grid>
    </Grid>
</UserControl>
